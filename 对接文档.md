# 会员管理前后端对接交付文档

## 一、对接目标

后端需要完成会员的分页查询、新增、详情、修改、逻辑删除/停用、手机号查询、消费轨迹查询和会员等级自动评定，并确保返回字段与本文档一致。

本次最终范围不包含“会员标签”。前端已删除会员标签的录入、展示和接口字段，后端无需设计或返回标签数据。

前端已经完成：

- 列表查询、关键字搜索、状态筛选及分页；
- 新增、编辑、删除会员；
- 会员详情和消费轨迹；
- 手机号、生日、初始积分和状态录入；
- 手机号格式、必填项和初始积分非负校验；
- 近一年消费额、自动等级和基础折扣展示；
- 普卡 98 折、银卡 95 折、金卡 9 折的规则提示。

注意：等级已从新增/编辑表单中移除，前端只展示后端评定结果，不允许人工指定或修改会员等级。

前端文件：

- `src/views/member/MemberListView.vue`
- `src/api/member.ts`
- `src/types/member.ts`

默认接口前缀为 `http://localhost:8080/api`，以下路径均省略 `/api`。

## 二、统一响应格式

所有接口必须返回统一结构，HTTP 成功且业务成功时 `code` 为 `200`：

```json
{
  "code": 200,
  "message": "成功",
  "data": {}
}
```

分页接口的 `data` 必须使用以下字段，不要改成 `items、records、pageNum` 等其他命名：

```json
{
  "list": [],
  "total": 0,
  "page": 1,
  "size": 10
}
```

字段统一使用 camelCase。日期建议使用 ISO 8601，例如 `2000-01-01`；日期时间使用 `2026-07-06T10:00:00`。

## 三、完整会员返回对象

列表、创建成功、详情、修改成功、手机号查询均应返回相同的会员字段：

```json
{
  "memberId": 1,
  "memberName": "赵六",
  "phone": "13600000000",
  "gender": "女",
  "birthday": "2000-01-01",
  "levelName": "普卡",
  "discountRate": 0.98,
  "points": 120,
  "totalAmount": 588.50,
  "recentYearAmount": 588.50,
  "registerTime": "2026-07-06T10:00:00",
  "status": "启用"
}
```

其中：

| 字段 | 类型 | 是否必须返回 | 说明 |
| --- | --- | --- | --- |
| `memberId` | number | 是 | 会员主键 |
| `memberName` | string | 是 | 会员姓名 |
| `phone` | string | 是 | 完整手机号，前端列表负责脱敏 |
| `gender` | string/null | 否 | `男、女、其他` |
| `birthday` | string/null | 否 | 生日，格式 `YYYY-MM-DD` |
| `levelName` | string | 是 | 后端自动评定，只能为 `普卡、银卡、金卡` |
| `discountRate` | number | 是 | 基础折扣率：普卡 `0.98`、银卡 `0.95`、金卡 `0.90` |
| `points` | number | 是 | 当前可用积分 |
| `totalAmount` | number | 是 | 累计消费金额，不是账户余额 |
| `recentYearAmount` | number | 是 | 会员近一年有效消费实付总额，作为等级评定依据 |
| `registerTime` | string | 是 | 注册时间；数据库 `CREATE_TIME` 对外映射为该字段 |
| `status` | string | 是 | `启用、停用` |

原接口示例缺少的 `birthday` 需要补充；原数据库/实体若缺少 `status`，后端需要增加字段或提供稳定的计算/映射逻辑。

## 四、接口逐项对接要求

### 1. 分页查询会员

`GET /members?page=1&size=10&keyword=赵六&status=启用`

参数：

- `page`：从 1 开始；
- `size`：每页数量；
- `keyword`：可选，同时匹配会员姓名、完整手机号、会员编号；
- `status`：可选，值为 `启用` 或 `停用`。

成功返回：

```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "list": [
      {
        "memberId": 1,
        "memberName": "赵六",
        "phone": "13600000000",
        "gender": "女",
        "birthday": "2000-01-01",
        "levelName": "普卡",
        "discountRate": 0.98,
        "points": 120,
        "totalAmount": 588.50,
        "recentYearAmount": 588.50,
        "registerTime": "2026-07-06T10:00:00",
        "status": "启用"
      }
    ],
    "total": 1,
    "page": 1,
    "size": 10
  }
}
```

### 2. 新增会员

`POST /members`

完整请求体：

```json
{
  "memberName": "赵六",
  "phone": "13600000000",
  "gender": "女",
  "birthday": "2000-01-01",
  "initialPoints": 120,
  "status": "启用"
}
```

请求字段要求：

- `memberName、phone` 必填；
- `gender、birthday、status` 可选，后端应设置合理默认值；
- `initialPoints` 可选，默认 `0`，必须大于等于 `0`；
- 不接收 `tags` 或其他会员标签字段。
- 不接收 `levelName` 或 `discountRate`；新会员等级由后端按规则初始化，通常为普卡。

成功时 `data` 返回第三节定义的完整会员对象，不能只返回 `memberId`。

新增事务要求：保存会员后，如果 `initialPoints > 0`，必须同时写入一条积分流水，变动类型可定义为“开户赠送/初始积分”，并保证会员积分余额与积分流水在同一事务中提交。

### 3. 查询会员详情

`GET /members/{memberId}`

成功时 `data` 返回第三节的完整会员对象，包括 `birthday、points、totalAmount、recentYearAmount、levelName、discountRate、registerTime、status`。

### 4. 修改会员

`PUT /members/{memberId}`

请求体：

```json
{
  "memberName": "赵六",
  "phone": "13600000000",
  "gender": "女",
  "birthday": "2000-01-01",
  "status": "启用"
}
```

修改接口不接收 `initialPoints、levelName、discountRate`，不得通过基础资料修改覆盖当前积分或等级。积分变动应通过独立积分业务并记录流水。成功时 `data` 返回更新后的完整会员对象。

### 5. 删除会员

`DELETE /members/{memberId}`

成功返回：

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

建议实现为逻辑删除：把 `status` 更新为 `停用`，不要物理删除会员，也不要级联删除销售订单、退货记录或积分流水。

### 6. 根据手机号查询会员

`GET /members/phone/{phone}`

手机号必须精确匹配。成功时 `data` 返回完整会员对象，供 POS 收银识别会员使用。

### 7. 查询会员消费轨迹

`GET /members/{memberId}/orders?page=1&size=8`

成功返回：

```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "list": [
      {
        "saleId": 3001,
        "saleNo": "SO202607290001",
        "saleDate": "2026-07-29T14:20:00",
        "totalAmount": 299.00,
        "discountAmount": 20.00,
        "paidAmount": 279.00,
        "payType": "微信支付",
        "status": "已完成",
        "pointsChange": 279
      }
    ],
    "total": 1,
    "page": 1,
    "size": 8
  }
}
```

订单按 `saleDate` 倒序排列。目前前端展示订单号、消费时间、支付方式、实付金额和订单状态，其余字段保留供后续扩展。

## 五、等级自动评定业务逻辑

### 1. 固定等级与折扣映射

| 等级 | `levelName` | `discountRate` | 页面显示 |
| --- | --- | --- | --- |
| 普卡 | `普卡` | `0.98` | 98折 |
| 银卡 | `银卡` | `0.95` | 95折 |
| 金卡 | `金卡` | `0.90` | 9折 |

`discountRate` 建议由后端根据 `levelName` 统一映射并返回，POS 结算也必须使用后端的折扣结果，不能信任前端提交的折扣率。

### 2. 消费统计口径

`recentYearAmount` 应由后端计算，建议统一口径为：

- 时间范围：计算时点向前滚动一年；
- 只统计已完成/已支付的销售订单；
- 金额使用订单实际支付金额 `paidAmount`；
- 已取消、支付失败的订单不计入；
- 已完成退货的退款金额必须从有效消费额中扣除；
- 结果最低为 `0`，金额计算使用 decimal，不能使用浮点数累计。

### 3. 等级门槛（联调前必须补充）

当前需求没有给出银卡和金卡的年度消费门槛，因此前端没有写死任何阈值。后端实现前必须由项目组确认：

- `silverThreshold`：达到多少元升级为银卡；
- `goldThreshold`：达到多少元升级为金卡；
- 必须满足 `0 <= silverThreshold < goldThreshold`。

推荐将门槛做成后端配置，而不是散落在 Controller 或前端代码中。判定形式为：

```text
recentYearAmount >= goldThreshold    => 金卡
recentYearAmount >= silverThreshold  => 银卡
其他                                 => 普卡
```

### 4. 重算时机

后端应把“计算近一年消费额 + 判定等级”封装为同一个领域服务，并至少在以下时机调用：

1. 销售订单支付完成后；
2. 退货退款完成后；
3. 历史订单被修正或撤销后；
4. 每日定时批量重算一次，处理旧消费滑出近一年窗口导致的等级变化。

等级和订单状态变更应保证事务一致性。并发订单结算时，需要避免旧计算结果覆盖新结果。

### 5. 升级与降级

“近一年滚动消费额自动判定并调整”通常意味着既能升级也能降级。当前前端可展示后端返回的任意最新等级；项目组仍需确认是否存在“只升不降”、保级期或降级缓冲期。如果没有额外规则，后端按可升可降实现。

### 6. 是否需要保存等级

可以选择：

- 保存 `LEVEL_NAME` 并在上述时机更新，查询更快；或
- 查询时实时计算，数据更实时但列表查询成本更高。

无论采用哪种方式，`levelName、discountRate、recentYearAmount` 必须来自同一次评定结果，不能出现“等级是银卡但折扣率是 0.98”的不一致数据。如保存等级，建议同时记录等级最后评定时间，便于排查。

## 六、后端必须完成的工作清单

- [ ] 新建会员 Controller、Service、DTO，并实现上述 7 个接口；
- [ ] `MemberDto` 增加 `birthday`；新增 DTO 增加 `initialPoints`，移除或忽略人工传入的 `levelName、discountRate`；
- [ ] 会员返回 DTO 增加 `birthday、points、totalAmount、recentYearAmount、levelName、discountRate、registerTime、status`；
- [ ] 将数据库 `CREATE_TIME` 映射为接口字段 `registerTime`；
- [ ] 如果会员表没有 `STATUS`，增加该字段，默认值设为 `启用`；
- [ ] 手机号增加唯一性校验，并在新增和修改时都处理冲突；
- [ ] 初始积分和积分流水使用数据库事务；
- [ ] 删除使用逻辑删除/停用，并保留会员历史业务数据；
- [ ] 消费轨迹关联 `SALE_ORDER`，按消费时间倒序分页；
- [ ] 项目组确认 `silverThreshold、goldThreshold`，并放入后端统一配置；
- [ ] 实现近一年有效消费额计算，正确处理取消订单和退货退款；
- [ ] 实现普卡 0.98、银卡 0.95、金卡 0.90 的统一折扣映射；
- [ ] 在支付、退款、订单修正后触发单会员等级重算；
- [ ] 增加每日批量重算任务，处理滚动时间窗口造成的等级变化；
- [ ] 确认是否允许降级；未提供额外规则时按可升可降实现；
- [ ] POS 结算从后端会员结果取得折扣，不接受前端自报等级或折扣率；
- [ ] 所有返回使用 `{ code, message, data }`，分页字段固定为 `{ list, total, page, size }`；
- [ ] 不实现、不接收、不返回会员标签字段。

## 七、建议错误返回

建议后端使用明确的业务消息，便于联调定位：

| 场景 | 建议 HTTP 状态 | message |
| --- | --- | --- |
| 参数校验失败 | 400 | `请求参数不合法` |
| 手机号已存在 | 409 | `手机号已注册` |
| 会员不存在 | 404 | `会员不存在` |
| 会员已停用 | 409 | `会员已停用` |
| 服务端异常 | 500 | `服务器内部错误` |

如果项目统一规定业务错误仍返回 HTTP 200，则至少保证 `code != 200` 且 `message` 能准确说明原因。

## 八、联调验收用例

1. 新增带生日和初始积分的会员，确认会员积分与积分流水一致；
2. 使用姓名、手机号和会员编号分别搜索；
3. 使用“启用/停用”筛选并验证分页总数；
4. 修改姓名、手机号、生日和状态，确认积分、等级不被请求体覆盖；
5. 删除会员后确认状态变为停用，历史订单仍可查询；
6. 查询无消费记录的会员，返回空 `list` 而不是 `null`；
7. 验证重复手机号、无效会员 ID 和分页越界场景；
8. 构造普卡、银卡、金卡门槛边界金额，确认等级和折扣完全匹配；
9. 完成一笔销售后确认 `recentYearAmount` 和等级及时更新；
10. 完成退货后确认消费额扣减，并验证必要时发生降级；
11. 模拟旧订单滑出一年窗口，验证定时任务重新评定等级。

## 九、项目级说明

会员模块的 TypeScript 检查已通过。仓库中仍有其他业务路由对应的空 `.vue` 文件，会导致整个项目 Vite 生产构建失败；这些文件不属于本次会员管理前端交付范围。
