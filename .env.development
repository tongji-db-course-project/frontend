# ===== 前端环境变量·开发模式覆盖（pnpm dev 使用，可提交仓库） =====
# 说明：
# - 仅在开发模式加载，覆盖 .env 中的同名变量。
# - 开发时走相对路径 /api，由 Vite dev server 代理到后端（见 vite.config.ts:
#   proxy['/api'].target = http://localhost:5113，并 rewrite 去掉 /api 前缀）。
#   最终 /api/auth/login → http://localhost:5113/auth/login。

# --- 接口地址 ---
# 走 Vite 代理，避免浏览器跨域；改后端端口只需同步 vite.config.ts 的 target。
VITE_API_BASE_URL=/api

# 登录接口路径
VITE_AUTH_LOGIN_PATH=/auth/login

# --- Mock 开关（默认全部关闭，统一走真实后端） ---
# 登录及通用本地 Mock
VITE_USE_LOCAL_MOCK=false
# 角色管理模块本地 Mock
VITE_USE_ROLE_LOCAL_MOCK=false
